<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.economics">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme_formal">
        <activity
            android:name=".activity_elasticity"
            android:label="elasticity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".activity_taxes"
            android:label="taxes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity_ktv"
            android:label="ktv"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity_kpv"
            android:label="@string/title_activity_kpv"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>